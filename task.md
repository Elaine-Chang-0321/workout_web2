# 任務說明 (index.html)

## 1. 建立基本 HTML 結構
- 建立一個名為 [`index.html`](index.html) 的文件。
- 引入基本的 HTML5 結構，包括 `<!DOCTYPE html>`、[`<html>`](index.html)、[`<head>`](index.html) 和 [`<body>`](index.html) 標籤。
- 設定頁面標題為 "我的運動網頁" (`<title>我的運動網頁</title>`)。
- 設定字元編碼為 UTF-8 (`<meta charset="UTF-8">`)。
- 加入 viewport meta 標籤以支援響應式設計 (`<meta name="viewport" content="width=device-width, initial-scale=1.0">`)。

## 2. 連結外部樣式表
- 在 [`<head>`](index.html) 中連結一個外部 CSS 檔案，例如 [`style.css`](style.css) (`<link rel="stylesheet" href="style.css">`)。

## 3. 連結外部 JavaScript 檔案
- 在 [`<body>`](index.html) 結束標籤前連結一個外部 JavaScript 檔案，例如 [`script.js`](script.js) (`<script src="script.js" defer></script>`)。

## 4. 建立導覽列 (Navigation Bar)
- 在 [`<body>`](index.html) 中建立一個 [`<nav>`](index.html) 標籤。
- 在 [`<nav>`](index.html) 中加入至少三個連結，例如 "首頁"、"訓練計畫"、"關於我們"。

## 5. 建立頁首 (Header)
- 在 [`<body>`](index.html) 中建立一個 [`<header>`](index.html) 標籤。
- 在 [`<header>`](index.html) 中加入一個主要標題 `<h1>`，例如 "歡迎來到我的運動網頁"。

## 6. 建立主要內容區 (Main Content Area)
- 在 [`<body>`](index.html) 中建立一個 [`<main>`](index.html) 標籤。
- 在 [`<main>`](index.html) 中加入至少兩個 `<div>` 區塊。
- 每個 `<div>` 區塊應包含一個次要標題 `<h2>` 和一些段落文字 `<p>`。

## 7. 建立頁尾 (Footer)
- 在 [`<body>`](index.html) 中建立一個 [`<footer>`](index.html) 標籤。
- 在 [`<footer>`](index.html) 中加入版權資訊，例如 "© 2023 我的運動網頁"。

## 8. 語義化 HTML 與可訪問性
- 確保盡可能使用語義化 HTML5 標籤。
- 確保所有圖片（如果有的話）都包含 `alt` 屬性。
- 確保所有連結都有明確的文字描述。